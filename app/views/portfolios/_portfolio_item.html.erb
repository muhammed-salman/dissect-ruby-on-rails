<div class="card">
  <%= image_tag portfolio_item.thumb_images unless portfolio_item.thumb_images.nil? %>
  <p class="card-text">
    <span><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item) %></span> <%= portfolio_item.subtitle %>
  </p>
</div>